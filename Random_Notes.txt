Dialogue displays
	Regular dialogue
	Intro dialogue and choices
	Dramatic text/transition dialogue

Should do all the logic in the dialogue manager.
Such as just storing what type of logic check to do and running the check in the manager.

Store variables as a class object in dialogue manager.
Create and edit the objects with visual editor.
Manager handles local variables while singleton handles them across multiple scenes.
Dialogue logic node is being left unfinished until the dialogue variable node works.

Redo Dialogue logic so beneath variable type is variable picker which when picked shows the logic options.
